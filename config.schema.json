{
  "pluginAlias": "DysonSolarcycleMorph",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Controls the Bluetooth-only Dyson Solarcycle / Lightcycle Morph.\n\n**The lamp must be registered in the MyDyson app.** Its key is issued by Dyson against the account the lamp belongs to and cannot be read from the device itself.",
  "schema": {
    "type": "object",
    "properties": {
      "adapter": {
        "title": "Bluetooth adapter",
        "type": "string",
        "required": false,
        "placeholder": "hci0",
        "description": "Leave empty to use the system default adapter."
      },
      "lights": {
        "title": "Lights",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "title": "Name",
              "type": "string",
              "required": true,
              "default": "Dyson Morph"
            },
            "mac": {
              "title": "BLE MAC address",
              "type": "string",
              "required": true,
              "placeholder": "AA:BB:CC:DD:EE:FF",
              "pattern": "^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$"
            },
            "serial": {
              "title": "Serial number",
              "type": "string",
              "required": true,
              "description": "Printed on the lamp base and shown in the MyDyson app."
            },
            "motionSensor": {
              "title": "Expose the motion sensor",
              "type": "boolean",
              "default": false,
              "description": "Adds the lamp's built-in motion detector as a HomeKit motion sensor."
            }
          }
        }
      }
    }
  },
  "customUi": true,
  "customUiPath": "./homebridge-ui",
  "footerDisplay": "Keys are stored outside config.json, in the plugin's storage directory."
}
