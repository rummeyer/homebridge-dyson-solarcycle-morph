/** Must match the `platform` name used in Homebridge's config.json. */
export const PLATFORM_NAME = 'DysonSolarcycleMorph';

/** Must match the `name` field in package.json. */
export const PLUGIN_NAME = 'homebridge-dyson-solarcycle-morph';
